<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-b from-blue-300 to-blue-100 min-h-screen flex items-center justify-center font-sans text-indigo-900">

  <div
    class="relative bg-white p-10 rounded-3xl shadow-lg w-80 sm:w-96 text-center text-indigo-800 font-semibold drop-shadow-md">
    <a href="/" title="Close and go home"
      class="absolute top-4 right-4 text-3xl font-bold text-blue-400 hover:text-blue-600 transition rounded-full cursor-pointer select-none">&times;</a>

    <h2 class="text-3xl font-extrabold mb-5 text-indigo-700">Login</h2>
    <p class="mb-8 text-indigo-600 font-semibold">Welcome to <strong class="font-bold">MyBlog</strong>! Please log in to continue.</p>

    <form method="POST" action="/login" autocomplete="off" class="space-y-6 text-left">
      <div>
        <label for="email" class="block mb-2 text-sm font-semibold text-indigo-700">Email</label>
        <input id="email" name="email" type="email" required placeholder="you@example.com"
          class="w-full px-4 py-3 rounded-xl border-2 border-blue-300 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition" />
      </div>

      <div>
        <label for="password" class="block mb-2 text-sm font-semibold text-indigo-700">Password</label>
        <input id="password" name="password" type="password" required placeholder="Your password"
          class="w-full px-4 py-3 rounded-xl border-2 border-blue-300 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:border-blue-500 transition" />
      </div>

      <button type="submit"
        class="w-full py-3 bg-blue-600 text-white text-lg font-bold rounded-2xl hover:bg-blue-700 shadow-md transition">
        Login
      </button>
    </form>

    <p class="mt-8 text-indigo-700 font-semibold">New here? Register to get started!</p>
    <a href="/register" class="inline-block mt-4 w-full">
      <button
        class="w-full py-3 bg-blue-300 text-indigo-900 font-bold rounded-2xl hover:bg-blue-400 hover:text-white shadow-md transition">
        Register
      </button>
    </a>
  </div>

</body>

</html>
